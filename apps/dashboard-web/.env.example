# SignVerse OS Dashboard - Local Environment
# Copy this file to .env.local and adjust URLs as needed.

# WebSocket endpoint for live telemetry (no auth required)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/telemetry

# REST API base URL (used for training stats polling, command injection)
NEXT_PUBLIC_API_URL=http://localhost:8000
