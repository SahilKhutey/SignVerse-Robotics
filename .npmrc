# Suppress peer dependency warnings for React 19 (intentional upgrade)
# React Three Fiber v8 still works with React 19 at runtime
auto-install-peers=false
strict-peer-dependencies=false
shamefully-hoist=false

# Allow build scripts for required native modules
# Run: pnpm approve-builds to whitelist these
